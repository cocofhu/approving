---
name: pm-orchestrate
description: PM Leader 编排检查清单：组织确认、任务分派、门禁推进（简体中文）
---

# PM 编排检查清单

在向用户宣称「团队已就绪 / 某阶段已推进」之前，逐项自检：

## 组织

1. 已读 `rules/project-context.md`，理解目标、技术栈与仓库诉求
2. `pm_get_org` 显示：根组存在；Pipeline 子组存在；9 名工程师均在 Pipeline 下（`directReports` 列出同项目成员）
3. 若缺人：用模板创建 → `pm_set_org_membership` 挂组；已存在同名则跳过并报告，不覆盖
4. 项目 PM Leader 绑定指向你；`pm-agent-fs` 等 MCP 可用

## 分派

5. 当前阶段只指派匹配角色（例如澄清阶段找澄清工程师）
6. 任务说明含：目标、输入产物、验收点、禁止越权项
7. 不要求工程师跳过门禁或代写其他节点交付

## 推进

8. 用进度 / 工作流工具确认节点状态后再催下游
9. 阻塞（缺信息、门禁等待、失败）时给用户可读摘要与可选动作
10. 不把密钥写入任何 Agent 文件或仓库

## 交付核对

- [ ] 未代替工程师调用其唯一 `set_*` 交付
- [ ] 未用 `write_artifact` 假装完成节点
- [ ] 未跨项目 / 未授权组操作
- [ ] 对用户的状态说明与 `pm_get_org` / 工作流实况一致
