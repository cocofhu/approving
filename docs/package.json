{
  "name": "grasp-docs",
  "private": true,
  "version": "0.1.0",
  "description": "Approving project site (static HTML) and help docs (Markdown)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "server": "node scripts/build.mjs && npx --yes serve public -p 4000"
  },
  "dependencies": {
    "markdown-it": "^14.1.0",
    "gray-matter": "^4.0.3"
  },
  "overrides": {
    "js-yaml": "3.15.2"
  },
  "engines": {
    "node": ">=20"
  }
}
